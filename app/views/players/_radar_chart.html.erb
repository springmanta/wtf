<svg viewBox="0 0 300 300" class="mx-auto w-full max-w-xs">
  <%= render "shared/radar_grid", labels: Player::SKILLS.values %>
  <polygon points="<%= radar_polygon_points(player.skills.values.map(&:to_f)) %>"
           fill="currentColor" class="text-emerald-500" fill-opacity="0.35" stroke="currentColor" stroke-width="2"/>
</svg>
